#lang racket

(require rackunit)

;; Instalar: Archivo -> Package Manager -> Package Source: quickcheck
(require quickcheck)

;; Para probar, descomente este comando y ponga el nombre de su archivo
;; (require "archivo-guia-alumno.rkt")

(
